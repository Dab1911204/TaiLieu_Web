mixin box-head(title, desc)
    div(class="box-head")
        h1(class="inner-title") #{title}
        div(class="inner-desc") #{desc}

mixin box-head2(title, desc)
    div(class="box-head")
        h1(class="inner-title") #{title}
        div(class="inner-desc") #{desc}