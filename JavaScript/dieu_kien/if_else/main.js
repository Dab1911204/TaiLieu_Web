var course = {
    name: 'JavaScript',
    coin: 250
};
var result = course.coin > 0 ? `${course.coin} Coins` : 'Miễn phí';
console.log(result);
var a = 1;
var b = 2;

var result = a > b ? 'a lớn hơn b' : 'a nhỏ hơn b';